<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dao.BackstageDao">
   
   <select id="select_User" resultType="Backend_User">
   select *,valueName as userTypeName from backend_user a,data_dictionary c
   where a.userType=c.id
   AND userCode =#{name}
   <if test="pwd!=null">
     and userPassword=#{pwd}
   </if>
   </select>
</mapper>